import { UpdatedateDirective } from './updatedate.directive';

describe('UpdatedateDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdatedateDirective();
    expect(directive).toBeTruthy();
  });
});
